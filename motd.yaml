---
- name: Set motd on Linux systems
  hosts: all
  become: yes
  become_method: dzdo
  
  tasks:
    - name: "copy motd file to /etc/motd"
      copy:
        src: files/motd
        dest: /etc/motd
        owner: root
        group: root
        mode: '644'
